# buck_converter_transient_response
This is a simulation of the transient response of dc-dc buck converter, it should be noted that the input is PWM wave instead of step input, which makes the calculation complex.这是一个关于DC-DC buck转换器（降压转换器）瞬态响应的仿真。需要注意的是，输入信号是PWM波而非阶跃输入，这使得计算变得复杂
